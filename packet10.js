
document.addEventListener('DOMContentLoaded',async()=>{let d={};try{d=await fetch('content.json',{cache:'no-store'}).then(r=>r.json())}catch(e){console.error(e)}const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const lib=document.querySelector('[data-video-library]'),fil=document.querySelector('[data-video-filters]'),vid=d.videoLibrary||[],cats=['All',...new Set(vid.map(v=>v.category))];function render(c){if(!lib)return;const a=c==='All'?vid:vid.filter(v=>v.category===c);lib.innerHTML=a.map(v=>`<article class="video-card"><div class="video-thumb"><img src="${esc(v.image)}" alt=""><button class="video-play" type="button">▶</button><span class="video-duration">${esc(v.duration)}</span></div><div class="video-copy"><span>${esc(v.category)}</span><h3>${esc(v.title)}</h3><div class="video-meta"><b>${esc(v.platform)}</b><b>${esc(v.views)} views</b></div></div></article>`).join('');document.querySelectorAll('.video-play').forEach(b=>b.onclick=()=>alert('Connect this card to the final hosted video URL before launch.'))}if(fil){fil.innerHTML=cats.map((c,i)=>`<button class="${i?'':'active'}" data-video-filter="${esc(c)}">${esc(c)}</button>`).join('');fil.querySelectorAll('button').forEach(b=>b.onclick=()=>{fil.querySelectorAll('button').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(b.dataset.videoFilter)})}render('All');const h=document.querySelector('[data-health-grid]');if(h)h.innerHTML=(d.siteHealth?.labels||[]).map(x=>`<article class="health-card"><span>${esc(x.name)}</span><strong>${esc(x.score)}</strong><div class="health-bar"><i style="width:${Number(x.score)}%"></i></div></article>`).join('');const c=document.querySelector('[data-launch-checklist]');if(c)c.innerHTML=(d.launchChecklist||[]).map(x=>`<div class="check-item ${esc(x.status)}"><span class="check-icon">${x.status==='complete'?'✓':'!'}</span><div><strong>${esc(x.area)}</strong><br><span>${esc(x.item)}</span></div><span class="check-status">${esc(x.status)}</span></div>`).join('');document.querySelector('[data-run-launch-check]')?.addEventListener('click',()=>{const p=(d.launchChecklist||[]).filter(x=>x.status!=='complete').length;alert(p?`${p} launch items still need attention.`:'Launch checklist complete.')})});
